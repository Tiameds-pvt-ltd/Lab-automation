#name: CI/CD Pipeline
#
#on:
#  push:
#    branches:
#      - main
#  pull_request:
#    branches:
#      - main
#
#jobs:
#  build:
#    runs-on: ubuntu-latest
#    steps:
#      # Step 1: Checkout the code
#      - name: Checkout code
#        uses: actions/checkout@v3
#
#      # Step 2: Set up JDK 17 for building the Spring Boot application
#      - name: Set up JDK 17
#        uses: actions/setup-java@v3
#        with:
#          java-version: '17'
#          distribution: 'temurin'
#
#      # Step 3: Build with Maven
#      - name: Build with Maven
#        env:
#          DB_URL: ${{ secrets.DB_URL }}
#          DB_USERNAME: ${{ secrets.DB_USERNAME }}
#          DB_PASSWORD: ${{ secrets.DB_PASSWORD }}
#          JWT_SECRET: ${{ secrets.JWT_SECRET }}
#        run: mvn clean install --no-transfer-progress
#
#      # Step 4: Set up Docker Buildx for multi-platform support
#      - name: Set up Docker Buildx
#        uses: docker/setup-buildx-action@v1
#
#      # Step 5: Cache Docker layers
#      - name: Cache Docker layers
#        uses: actions/cache@v2
#        with:
#          path: /tmp/.buildx-cache
#          key: ${{ runner.os }}-buildx-${{ github.sha }}
#          restore-keys: |
#            ${{ runner.os }}-buildx-
#
#      # Step 6: Build Docker image
#      - name: Build Docker image
#        run: |
#          docker build -t abhishek94733/tiameds:${{ github.sha }} .
#
#      # Step 7: Log in to Docker Hub
#      - name: Log in to Docker Hub
#        uses: docker/login-action@v3
#        with:
#          username: ${{ secrets.DOCKER_USERNAME }}
#          password: ${{ secrets.DOCKER_PASSWORD }}
#
#      # Step 8: Push Docker image to Docker Hub
#      - name: Push Docker image to Docker Hub
#        run: |
#          docker push abhishek94733/tiameds:${{ github.sha }}
#
#      # Step 9: Log in to AWS ECR
#      - name: Log in to AWS ECR
#        uses: aws-actions/amazon-ecr-login@v1
#        with:
#          registry-type: private
#        env:
#          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
#          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
#          AWS_REGION: ${{ secrets.AWS_REGION }}
#
#      # Step 10: Tag Docker image for ECR
#      - name: Tag Docker image for ECR
#        run: |
#          docker tag abhishek94733/tiameds:${{ github.sha }} ${{ secrets.AWS_ECR_URI }}:${{ github.sha }}
#
#      # Step 11: Push Docker image to ECR
#      - name: Push Docker image to ECR
#        run: |
#          docker push ${{ secrets.AWS_ECR_URI }}:${{ github.sha }}
#
#      # Step 12: Fetch ECS task definition and update the image with the new ECR image
#      - name: Update ECS Task Definition
#        run: |
#          # Fetch the current ECS task definition
#          TASK_DEF=$(aws ecs describe-task-definition --task-definition lab_automation --region ${{ secrets.AWS_REGION }})
#
#          # Remove unwanted fields like taskDefinitionArn and requiresAttributes
#          NEW_TASK_DEF=$(echo "$TASK_DEF" | jq 'del(.taskDefinitionArn, .requiresAttributes, .status)')
#
#          # Update the image in containerDefinitions
#          NEW_TASK_DEF=$(echo "$NEW_TASK_DEF" | jq '.containerDefinitions[0].image = "${{ secrets.AWS_ECR_URI }}:${{ github.sha }}"')
#
#          # Debugging step: print the updated task definition
#          echo "NEW_TASK_DEF: $NEW_TASK_DEF"
#
#          # Save the updated task definition to a file
#          if [ -z "$NEW_TASK_DEF" ]; then
#            echo "Error: New task definition is empty."
#            exit 1
#          fi
#          echo "$NEW_TASK_DEF" > updated-task-definition.json
#
#          # Register the updated ECS task definition with 'family' and 'containerDefinitions'
#          REGISTERED_TASK_DEF=$(aws ecs register-task-definition \
#            --family lab_automation \
#            --cli-input-json file://updated-task-definition.json \
#            --region ${{ secrets.AWS_REGION }})
#
#          # Extract the task definition ARN or revision for use in the deploy step
#          TASK_DEF_REVISION=$(echo "$REGISTERED_TASK_DEF" | jq -r .taskDefinition.taskDefinitionArn)
#
#          echo "TASK_DEF_REVISION: $TASK_DEF_REVISION"
#          echo "TASK_DEF_REVISION=$TASK_DEF_REVISION" >> $GITHUB_ENV
#
#        env:
#          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
#          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
#          AWS_REGION: ${{ secrets.AWS_REGION }}
#
#      # Step 13: Deploy to ECS using the new task definition
#      - name: Deploy to ECS
#        uses: aws-actions/amazon-ecs-deploy-task-definition@v1
#        with:
#          cluster: lab_automation
#          service: lab_deploy
#          task-definition: ${{ env.TASK_DEF_REVISION }}  # Use the dynamically fetched task definition revision
#          wait-for-service-stability: true
#        env:
#          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
#          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
#          AWS_REGION: ${{ secrets.AWS_REGION }}
#
#
#


#=====================================================================
name: Deploy to ECS

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v2

      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Build JAR with Maven
        run: |
          mvn clean install -DskipTests

      - name: Log in to Amazon ECR
        uses: aws-actions/amazon-ecr-login@v1
        with:
          registry-type: private
        env:
          AWS_REGION: ${{ secrets.AWS_REGION }}
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}

      - name: Create ECR repository (if not exists)
        run: |
          aws ecr describe-repositories --repository-name lab_automation || aws ecr create-repository --repository-name lab_automation
        env:
          AWS_REGION: ${{ secrets.AWS_REGION }}
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}

      - name: Build Docker image
        run: |
          docker build -t 195275675833.dkr.ecr.us-east-1.amazonaws.com/lab_automation:$GITHUB_SHA .

      - name: Push Docker image to ECR
        run: |
          docker push 195275675833.dkr.ecr.us-east-1.amazonaws.com/lab_automation:$GITHUB_SHA
        env:
          AWS_REGION: ${{ secrets.AWS_REGION }}
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}

      - name: Update ECS task definition
        id: task-def
        run: |
          ecs_task_def=$(cat <<EOF
          {
            "family": "lab_automation",
            "requiresCompatibilities": [
              "FARGATE"
            ],
            "networkMode": "awsvpc",
            "cpu": "256",
            "memory": "512",
            "containerDefinitions": [
              {
                "name": "lab_automation",
                "image": "195275675833.dkr.ecr.us-east-1.amazonaws.com/lab_automation:${GITHUB_SHA}",
                "memory": 512,
                "cpu": 256,
                "essential": true,
                "environment": [
                  {
                    "name": "DB_URL",
                    "value": "${{ secrets.DB_URL }}"
                  },
                  {
                    "name": "DB_USERNAME",
                    "value": "${{ secrets.DB_USERNAME }}"
                  },
                  {
                    "name": "DB_PASSWORD",
                    "value": "${{ secrets.DB_PASSWORD }}"
                  },
                  {
                    "name": "JWT_SECRET",
                    "value": "${{ secrets.JWT_SECRET }}"
                  }
                ],
                "portMappings": [
                  {
                    "containerPort": 8080,
                    "hostPort": 8080
                  }
                ]
              }
            ]
          }
          EOF
          )
          
          echo "$ecs_task_def" > task-definition.json

      - name: Register ECS task definition
        run: |
          aws ecs register-task-definition \
            --cli-input-json file://task-definition.json \
            --region ${{ secrets.AWS_REGION }}  # Explicitly specify the region
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_REGION: ${{ secrets.AWS_REGION }}

      - name: Deploy to ECS service
        run: |
          aws ecs update-service \
            --cluster lab_automation \
            --service lab_deploy \
            --task-definition lab_automation \
            --launch-type FARGATE \
            --network-configuration "awsvpcConfiguration={subnets=[subnet-xxxxxx],securityGroups=[sg-xxxxxx],assignPublicIp=ENABLED}" \
            --region ${{ secrets.AWS_REGION }}  # Explicitly specify the region
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_REGION: ${{ secrets.AWS_REGION }}
